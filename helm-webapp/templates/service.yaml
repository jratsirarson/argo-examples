apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.appName }}
  labels:
    app: {{ .Values.appName }}
spec:
  ports:
  - port: 3000
    protocol: TCP
    name: node
  selector:
    app: {{ .Values.appName }}
    tier: backend
  type: NodePort
